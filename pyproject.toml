[project]
name = "rhino-renderer-client-examples"
version = "0.1.0"
description = "Python examples for the Rhino Renderer C# plugin"
requires-python = ">=3.10"
dependencies = [
  "rhino3dm>=8.0.0",
]

[tool.uv]
package = false
